/**
 * Legacy file intentionally sanitized.
 *
 * Active Cloud Functions source is TypeScript under `functions/src/**`
 * and compiles to `functions/lib/index.js` (see package.json main).
 */

module.exports = {};
